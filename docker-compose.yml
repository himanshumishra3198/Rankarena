# docker-compose.yml
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: rankarena
      POSTGRES_PASSWORD: password
    ports: ["5432:5432"]

  redis:
    image: redis:7
    ports: ["6379:6379"]
